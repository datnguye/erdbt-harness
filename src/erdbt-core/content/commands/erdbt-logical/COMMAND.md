---
description: "Phase 3 — expand the concept model into attributes, keys, and cardinality."
argument-hint: "[--project <path-to-dbt-project>]"
---

Run phase 3 of the erdbt harness using the `erdbt-logical` skill.

Target dbt project: $ARGUMENTS

Write `erdbt/logical.ir.yaml` with attributes, keys, and resolved cardinality,
following the naming conventions already used in the target project.

Stop at the gate for review.
